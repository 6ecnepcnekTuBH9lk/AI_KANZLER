# Приёмка KANZLER Business Platform

## Точечная доработка ТН ВЭД: TENCEL и Excel — 08.09.2026

Живой прогон Alta: **19 CONFIRMED / 38 UNRESOLVED / 0 ошибок источника**.
Относительно предыдущих 17 подтверждений изменились только:

| Строка Excel | Артикул | Было | Стало |
|---|---|---|---|
| 18 | 7S-211TD-0917-15 | UNRESOLVED | 6103390000 |
| 19 | 7S-231VL-0918-15 | UNRESOLVED | 6103490001 |

Остальные 17 кодов сохранены. TENCEL/Tencel/тенсел в составе нормализован в искусственное
целлюлозное волокно; джинсы в строке 13 остаются нерешёнными по независимым условиям ткани
и назначения. Строки 24–27 по-прежнему UNRESOLVED: производственное/профессиональное
назначение нельзя исключить по бренду. У лоферов 28–31 извлечены замшевый верх и резиновая
подошва из структуры состава; конструктивные критерии остаются в полном evidence.

Excel содержит F «Код ТНВЭД», L «Комментарий», **M (13-я колонка) «Источник»**.
19 подтверждений: текстовый десятизначный код, пустой комментарий, прямой URL карточки Alta.
38 уточнений: пустые код и источник, один короткий комментарий. `comment`, кандидаты,
предикаты и evidence остались в UI/API; для Excel добавлены `user_comment` и `source_url`.
Короткий вопрос обозначает ближайший недостающий признак; повторная классификация проверяет
все условия. Исходный комментарий хранится в API, формулы защищены от перезаписи.

Построчно сверены код, комментарий и источник с текущим `Коды ТНВЭД скилл.xlsx`.
Этот файл имеет другой SHA256 и обновлённые комментарии/заголовок источника; все 57 товаров
и коды совпали с подтверждённым историческим снимком. Коды и источники совпали в 53 строках;
отличия — только 24–27. Комментарии дословно совпали в 26 строках, остальные сокращены
или уточнены по фактически недостающим данным. Оба хеша записаны в протоколе.

Проверки: **229 backend-тестов**, Ruff check/format, **4 frontend-теста**, lint, typecheck,
build; затем живая Alta и OOXML-проверка всех 57 строк. Визуально проверены заголовок,
короткие комментарии и ссылки; ошибок формул в просмотренных данных нет.
Из ZIP изменены только sheet1.xml и styles.xml. Исходные товары/порядок, остальные части
архива и исходный файл сохранены. ClassificationEngine, apparel/footwear/conditions,
Alta-адаптер, frontend и коммерческая логика в этой доработке не изменены.

- [Итоговый Excel](../test-results/real-tnved/Артикулы_коллеции_ВЛ_27_Нет_кодов_ТНВЭД_2_с_кодами_ТНВЭД.xlsx).
- [Построчное сравнение со Skill и комментарии до/после](../test-results/real-tnved/skill-comparison-57.md).
- [Протокол проверки](../test-results/real-tnved/verification.json).

SHA256 результата: `065355b19b9d209ccf107d34d33fc02d69f3aabbcfc389b172b29e36b92ab738`.

## Предыдущая итерация ТН ВЭД — 08.09.2026 (исторические результаты)

Коммерческая бизнес-логика и пользовательский `start.ps1` не изменены.
Архитектура FastAPI + React + SQLAlchemy + SQLite сохранена.

| Набор | CONFIRMED | UNRESOLVED | Ошибки источника |
|---|---:|---:|---:|
| Исходный XLSX | 0 | 57 незаполненных | — |
| Предыдущий Skill, происхождение подтверждено пользователем | 23 | 34 | — |
| App_before, commit 787c689 | 13 | 44 | 0 |
| App_after, новый живой прогон Alta | 17 | 40 | 0 |

Основная причина для каждой из 40 строк (без двойного счёта): 8 — материалы обуви,
4 — конструкция обуви, 19 — нераскрытое волокно (16 микрофибра, 3 тенсел),
2 — деним/вельвет/назначение, 4 — производственное/профессиональное назначение,
3 — плотность трикотажа, облегание и ворот.

Изменились строки **16, 17, 18, 19, 20, 21, 22, 23, 54**:
6 новых подтверждений; 2 прежних подтверждения сняты; 1 код исправлен.
Пиджак и брюки в строках 18/19 больше не получают `6103490002`: промежуточная
ветвь/остаточные альтернативы проверяются, а волокно «тенсел» не угадывается.
Строка 54 исправлена с `6201200000` на `6201400000` по правилу смеси XI.2.

От эталона Skill отличаются **18, 19, 24, 25, 26, 27** — категория **E**:
недоказанная идентификация волокна либо предположение об отсутствии профессионального
назначения. Для доказательства кода по-прежнему не хватает данных. Код не выбран ради
совпадения счётчиков. Построчное решение и все поля признаков/кандидатов сохранены в отчётах.

Обувь: **0 подтверждений / 12 уточнений** на реальном входе. Это больше не безусловный
отказ engine: положительные тесты проходят для всех 6401–6405, включая материалы,
водонепроницаемость/крепление, защитный подносок, перфорации, длину стельки и высоту подошвы.
Реальным строкам недостающие признаки из тестовых примеров не подставляются.

Приёмка: **205 backend-тестов**, **4 frontend-теста**, Ruff check/format, lint/typecheck/build
прошли. После них выполнен новый сетевой прогон Alta и `verify_tnved_delivery.py`.
Все 57 семантических ожиданий совпали. Оригинал и предыдущий файл Skill не изменены.
Из ZIP изменены только `xl/worksheets/sheet1.xml` и `xl/styles.xml`; остальные части
побайтно идентичны. Исходные строки/ячейки, формулы, структура, старые стили сохранены;
коды — строки из 10 цифр; каждая незаполненная строка имеет предметную причину; XLSX открывается.

Результаты:

- `test-results/real-tnved/Артикулы_коллеции_ВЛ_27_Нет_кодов_ТНВЭД_2_с_кодами_ТНВЭД.xlsx`.
- [Сравнение со Skill — все 57 строк](../test-results/real-tnved/skill-comparison-57.md).
- [Полный differential с признаками и доказательствами](../test-results/real-tnved/differential-57.json).
- [Машинный протокол целостности и счётчиков](../test-results/real-tnved/verification.json).

Исходный SHA256: `d72bbd00df20b520b4cea2fe876981a0dd87f6f6931b08f5267b94c921a882d7`.
Результат SHA256: `b6538906102ef0aab143295b09d990bf951fae67b04a01bc42309d92f76a4954`.

Статусы каждого раздела Skill — [TNVED_RULES.md](TNVED_RULES.md): IMPLEMENTED,
PARTIAL, BLOCKED BY MISSING INPUT, BLOCKED BY UNIMPLEMENTED RULE, BLOCKED BY SOURCE.
Full parity не заявляется: детские позиции и новые/специальные неразобранные условия
не подтверждаются автоматически; auth-only codesearch не используется; визуальная печать
во всех корпоративных шаблонах Excel не проверялась. В реальных 57 строках после доработки
остались ограничения входных данных, а не безусловный запрет обуви или технических ветвей.

Изменённые исходники этой итерации:

- `backend/app/modules/tnved/alta.py`, `classifier.py`, `features.py`, `service.py`, `reporting.py`.
- Новые `apparel.py`, `footwear.py`, `conditions.py`, `branch_descriptions.py` в том же модуле.
- `backend/tests/test_tnved.py`; новые `test_tnved_rules.py` и `fixtures/tnved/{baseline.json,alta.json,expected.json,README.md}`.
- `frontend/src/App.tsx`, `App.test.tsx`: прозрачность карточки, без изменения коммерческих расчётов.
- `docs/TNVED_GAP_ANALYSIS.md`, `TNVED_RULES.md`, `ACCEPTANCE.md`, `TESTING.md`.
- Новые `scripts/capture_tnved_baseline.py`, `capture_tnved_sources.py`, `build_tnved_expectations.py`, `inspect_tnved_regression.py`, `verify_tnved_delivery.py`.
- Производные результаты живого прогона и отчёты в `test-results/real-tnved`.

## Предыдущая приёмка коммерческого модуля — 07.09.2026

Продолжение разработки касается только коммерческой аналитики. Сохранены FastAPI + React + SQLAlchemy + SQLite. Изменение пользователя в `start.ps1` сохранено. Авторизация, инфраструктура и модуль ТН ВЭД в этой итерации не перерабатывались.

## Реальный запуск

Входы: `ОЗ26_W33_план_сезона 28.08+фото.xlsx`, `План сезона ОЗ 26-27 из ценообразования.xlsx`, `Анализ продаж (Общий) (XLSX) 2026-09-06.xlsx`. Старый W35 не используется.

| Показатель | Результат |
|---|---:|
| Артикулы текущей коллекции | 338 |
| Известная база FACT | 223 артикула; 149 344 ед. |
| План на 06.09.2026 | 223 артикула; 3 611,34 ед. |
| Сезонный факт | 189 артикулов; 1 854 ед. |
| Сопоставимый набор | 189 артикулов; выполнение 53,6467% |
| Прогноз при однозначном сроке | 126 артикулов; 39 344,29 ед. |
| Статусы | РИСК49; НЕДОСТАТОЧНО ДАННЫХ289; остальные0 |
| Подтверждённая агрегатная размерная проблема | 181 артикул |
| Вторая поставка: подтверждённый риск | 0 |
| Условный недобор второй поставки, партия не подтверждена | 12 |
| Ценовых действий | 0 |
| Сообщений QA | 5 251 |

Суммы с разным покрытием не делятся друг на друга. 53,6467% относится только к сопоставимым 189 позициям. Прогноз с предсезонным темпом условный. Изменение числа планов обусловлено явным CATEGORY fallback; снижение уверенности статусов — исключением неутверждённых сроков наблюдения и агрегатной диагностикой, а не изменением факта продаж.

HTTP-запуск `a942e81d35744f30af79980e26187678`: три настоящих файла → фоновый расчёт → SQLite → API → скачанный XLSX. Все 338 строк по полям golden совпали с безопасной копией. SHA256 оригиналов до/после совпали; протокол `test-results/real-merchandise/http-verification.json`. Результат — `test-results/real-merchandise/KANZLER_Контроль_ассортимента_2026-09-06.xlsx`.

## Приёмка требований

| Требование нового задания | Статус | Результат |
|---|---|---|
| Предварительное чтение и gap map | IMPLEMENTED | [MERCHANDISE_GAP_ANALYSIS.md](MERCHANDISE_GAP_ANALYSIS.md) до изменений |
| Источники/база/полная нормированная траектория | IMPLEMENTED | W36–W8, границы месяцев, никакого переноса из-за позднего входа |
| Экономические нормативы | PARTIAL | Импорт и происхождение реализованы; правило экономического решения из всех отклонений не задано |
| NOS | PARTIAL | Отдельная стратегия и интерпретация; таблица конечных статусов отсутствует |
| Агрегатные размеры и операционные подтверждения | IMPLEMENTED | Доказательства, неизвестность, правильный порядок до цены |
| Конкретные магазины/размеры | BLOCKED BY MISSING INPUT DATA | Нет детализации; реконструкции нет |
| Вторая волна | PARTIAL | Все вычислимые показатели; фактические партии и условия экономики проверяются. Условный сценарий отделён |
| Выбор конкретных сроков/порогов из диапазонов | BLOCKED BY MISSING BUSINESS RULE | Нет автоматического выбора точки; QA/пустое значение/окно |
| Числовой baseline и объяснимость | IMPLEMENTED | Границы115/85/70, пять статусов, evidence/missing_evidence |
| Рекомендации/цена/даты | PARTIAL | Порядок и диапазоны есть; точные лимиты/даты только по утверждённому входу |
| Постоянная regression | IMPLEMENTED | 338 позиций, все требуемые поля, арифметический oracle, метаморфные тесты |
| UI/API/восемь листов | IMPLEMENTED | Один backend-результат; только новые поля прозрачности |
| Независимая экспертная эквивалентность всего Skill | BLOCKED BY MISSING BUSINESS RULE | Независимого эталона не предоставлено; неформализованные решения не объявлены реализованными |

Детализация каждого правила/раздела и нужных данных — [MERCHANDISE_RULES.md](MERCHANDISE_RULES.md). Проверки и команды — [TESTING.md](TESTING.md).

## Сохранённые результаты других модулей

Предыдущая приёмка ТН ВЭД: 57 строк, 13 кодов, 44 уточнения, 0 технических ошибок Alta. Эти результаты относятся к предыдущей итерации; сетевой классификатор повторно не запускался в рамках текущего задания. Общие тесты приложения, включая ТН ВЭД, выполняются в полном pytest.

## Запуск

Обычный запуск — `./start.ps1`. Для применения новых правил уже работающий backend нужно перезапустить; старые сохранённые результаты не пересчитываются автоматически. Новый анализ сохраняет отдельную запись истории.

## Изменённые файлы этой итерации

- `backend/app/modules/merchandise/importers.py`, `planning.py`, `metrics.py`, `decisions.py`, `service.py`, `reporting.py`.
- Новые `backend/app/modules/merchandise/norms.py`, `evidence.py`.
- `backend/tests/test_merchandise.py`; новые `test_merchandise_rules.py`, `test_merchandise_golden.py`, `fixtures/merchandise/inputs.json`, `fixtures/merchandise/expected.json`.
- `frontend/src/App.tsx`: только поля прозрачности карточки.
- `docs/MERCHANDISE_GAP_ANALYSIS.md`, `MERCHANDISE_RULES.md`, `ACCEPTANCE.md`, `TESTING.md`.
- Новые `scripts/merchandise_fixture.py`, `scripts/smoke_merchandise.py`.
- Производные результаты и протоколы в `test-results/real-merchandise`, `test-results/verification.json`; проверочный запуск в `test-results/ui-acceptance`.

79 backend-тестов и3 frontend-теста прошли. Ruff, frontend lint/typecheck/build — без ошибок. В браузере проверены история реального запуска, значения сводки, карточка, доказательства и экономические нормативы; разметка сохранена. 10 встроенных файлов побайтно совпали с ZIP. Пользовательский `start.ps1` в список наших изменений не входит.
